# PerpDex MVP 核心流程前端测试清单

> **版本**: 1.0
> **适用范围**: 前端功能逐项验证
> **目标闭环**: `Wallet Login -> Deposit -> Trade -> Position -> Withdraw`

---

## 测试环境准备

### 前置条件
- [ ] 前端服务运行在 http://localhost:3000
- [ ] 后端 API 运行在 http://localhost:3001
- [ ] PostgreSQL 数据库已启动
- [ ] Redis 已启动
- [ ] 拥有一个测试用的 MetaMask 钱包（Arbitrum Sepolia 网络）
- [ ] 钱包中有少量 SepoliaETH 用于支付 Gas
- [ ] Vault 合约已部署到 Arbitrum Sepolia

### 测试数据准备
```
测试钱包地址: (填入你的测试地址)
Vault 合约地址: (填入部署后的合约地址)
```

---

## 一、钱包连接与登录 (Authentication)

### 1.1 未登录状态首页
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 打开 http://localhost:3000 | 正常显示交易页面，右上角显示「连接钱包」按钮 | ⬜ |
| 观察页面布局 | 左侧：行情 + 图表 + 成交记录；右侧：下单面板 | ⬜ |
| 检查 MarketStats 组件 | 显示 BTC 最新价格、24h 涨跌、成交量 | ⬜ |
| 检查 PriceChart 区域 | 显示图表占位区域 | ⬜ |
| 检查 OrderForm 保证金输入 | 可用余额显示为 0 或连接提示 | ⬜ |

### 1.2 连接钱包流程
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 点击右上角「连接钱包」按钮 | 弹出 Reown AppKit 钱包选择弹窗 | ⬜ |
| 选择 MetaMask | MetaMask 扩展程序弹出连接请求 | ⬜ |
| 在 MetaMask 中确认连接 | AppKit 弹窗关闭，按钮显示钱包地址缩略 | ⬜ |
| 观察网络检查 | 如果未切换到 Arbitrum Sepolia，应有提示 | ⬜ |
| 切换到正确网络后 | 页面无报错，准备登录 | ⬜ |

### 1.3 SIWE 登录流程
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 连接钱包后点击登录 | 调用 `api.getAuthChallenge(address)` | ⬜ |
| MetaMask 弹出签名请求 | 显示 SIWE 格式消息 | ⬜ |
| 检查 SIWE 消息内容 | 包含 domain、address、nonce、chainId、issuedAt | ⬜ |
| 在 MetaMask 中签名 | 调用 `api.verifySignature()` | ⬜ |
| 登录成功 | 右上角显示钱包地址，进入已登录状态 | ⬜ |
| 检查 localStorage | 存储了 `accessToken` | ⬜ |
| 刷新页面 | 自动恢复登录状态，无需重新签名 | ⬜ |

### 1.4 登录状态校验
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 检查 `useAuth` hook 状态 | `isAuthenticated: true`, `user` 有数据 | ⬜ |
| 检查网络请求 | 调用 `GET /api/auth/session` 验证 token | ⬜ |
| Token 过期场景 | 自动清除 token，返回未登录状态 | ⬜ |

### 1.5 登出流程
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 点击登出按钮 | 调用 `api.logout()` | ⬜ |
| 检查 localStorage | `accessToken` 被清除 | ⬜ |
| 检查钱包连接 | MetaMask 保持连接，但需重新登录 | ⬜ |
| 页面状态 | 回到未登录状态，余额显示为 0 | ⬜ |

---

## 二、资产管理 (Assets)

### 2.1 未登录状态访问资产页
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 导航到 /assets | 显示「请先连接钱包并登录以查看资产」提示 | ⬜ |
| 检查页面布局 | 无数据表格，有登录引导文案 | ⬜ |

### 2.2 已登录状态资产概览
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 登录后访问 /assets | 调用 `GET /api/user/balance` | ⬜ |
| 检查余额卡片 | 显示：总权益、可用余额、已锁定 | ⬜ |
| 检查资产表格 | 列出 USDC、BTC、ETH 三种资产 | ⬜ |
| 检查 USDC 余额 | 总余额 = 可用 + 已锁定 | ⬜ |
| 隐藏零余额功能 | 勾选后过滤掉余额为 0 的资产 | ⬜ |

### 2.3 WebSocket 余额实时更新
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 建立 WebSocket 连接 | `socketClient.connect(token)` 成功 | ⬜ |
| 订阅余额更新 | `subscribeBalance(userId, callback)` | ⬜ |
| 在其他标签页开仓 | 本页面余额实时更新（锁定增加） | ⬜ |
| 关闭页面后重新打开 | WebSocket 自动重连并订阅 | ⬜ |

---

## 三、充值流程 (Deposit)

### 3.1 充值入口
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 资产页点击「充值」按钮 | 弹出充值弹窗或跳转到充值页 | ⬜ |
| 检查充值界面 | 显示：USDC 合约地址、充值说明 | ⬜ |

### 3.2 首次充值授权 (Approve)
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 输入充值金额（如 100 USDC） | 金额 >= 1 USDC 验证通过 | ⬜ |
| 点击「授权」按钮 | MetaMask 弹出 approve 交易 | ⬜ |
| 检查 approve 参数 | Spender 为 Vault 合约地址 | ⬜ |
| 在 MetaMask 确认 | 交易发送，显示 pending 状态 | ⬜ |
| 等待交易确认 | 显示 approve 成功，进入 deposit 步骤 | ⬜ |

### 3.3 存入 Vault (Deposit)
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 授权成功后点击「存入」 | MetaMask 弹出 deposit 交易 | ⬜ |
| 检查交易参数 | to: Vault 合约, value: 0, data: deposit 编码 | ⬜ |
| 在 MetaMask 确认 | 交易发送，显示 pending 状态 | ⬜ |
| 等待链上确认 | 交易成功后显示 confirmed 状态 | ⬜ |

### 3.4 Indexer 入账确认
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 链上 deposit 交易确认后 | Indexer 监听到 Deposit 事件 | ⬜ |
| 数据库更新 | `transactions` 表新增记录 | ⬜ |
| 余额更新 | `accounts.available_balance` 增加 | ⬜ |
| 前端显示 | 资产页余额自动更新（WebSocket 或轮询） | ⬜ |
| 交易历史 | /history 页面显示充值记录 | ⬜ |

---

## 四、交易功能 (Trading)

### 4.1 交易页面布局
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 访问 / (首页) | 显示交易页面 | ⬜ |
| 检查左侧区域 | MarketStats + PriceChart + RecentTrades | ⬜ |
| 检查右侧区域 | OrderForm + PositionPanel | ⬜ |
| 响应式布局 | 窗口缩小时布局自适应 | ⬜ |

### 4.2 下单表单 (OrderForm)
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 多空切换 | 点击「开多」/「开空」按钮切换 side | ⬜ |
| 切换样式 | 开多绿色高亮，开空红色高亮 | ⬜ |
| 保证金输入 | 输入数字，显示 USDC 后缀 | ⬜ |
| 可用余额提示 | 显示当前可用余额，带千分位 | ⬜ |
| 超额输入 | 输入 > 可用余额，提交按钮禁用 | ⬜ |
| 杠杆滑块 | 1x-100x 可调节，默认 10x | ⬜ |
| 订单估算 | 输入保证金后显示：仓位大小、开仓价、清算价、手续费 | ⬜ |

### 4.3 开仓流程 (市价单)
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 输入保证金（如 100 USDC） | 表单验证通过 | ⬜ |
| 选择杠杆（如 10x） | 杠杆值更新 | ⬜ |
| 点击「开多 BTC」 | 提交按钮显示 loading | ⬜ |
| 检查 API 请求 | `POST /api/trade/order` | ⬜ |
| 请求参数 | `{symbol: "BTC", side: "LONG", margin: "100000000", leverage: 10, size: ...}` | ⬜ |
| 后端处理 | TradeEngine 锁定保证金、创建订单、创建仓位 | ⬜ |
| 对冲任务 | BullMQ 创建 hedge task | ⬜ |
| API 响应 | 返回 `{order, position, hedgeTaskId}` | ⬜ |
| 前端显示 | Toast 提示开仓成功 | ⬜ |
| 余额更新 | 可用余额减少，锁定余额增加 | ⬜ |
| 仓位显示 | PositionPanel 显示新仓位 | ⬜ |

### 4.4 仓位展示 (PositionPanel)
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 检查仓位列表 | 显示：交易对、方向、杠杆、仓位大小 | ⬜ |
| 开仓价格 | 显示成交价 | ⬜ |
| 标记价格 | 实时更新（WebSocket 或轮询） | ⬜ |
| 未实现盈亏 (PnL) | 根据 markPrice 实时计算，正数绿色负数红色 | ⬜ |
| 清算价格 | 显示预估清算价 | ⬜ |
| 保证金比率 | 显示当前保证金率 | ⬜ |
| 风险等级 | Safe(绿色)/Warning(黄色)/Danger(红色) | ⬜ |

### 4.5 平仓流程
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 点击仓位「平仓」按钮 | 弹出确认弹窗 | ⬜ |
| 确认平仓 | 调用 `DELETE /api/trade/positions/:id` | ⬜ |
| 后端处理 | TradeEngine 计算已实现盈亏 | ⬜ |
| 保证金释放 | 锁定保证金 + 盈亏 转回可用 | ⬜ |
| 仓位状态 | 更新为 CLOSED | ⬜ |
| 对冲任务 | BullMQ 创建反向对冲任务 | ⬜ |
| API 响应 | 返回 `{order, realizedPnl, hedgeTaskId}` | ⬜ |
| 前端显示 | Toast 提示平仓成功，显示盈亏 | ⬜ |
| 仓位列表 | 该仓位消失或移到历史 | ⬜ |

### 4.6 持仓中价格更新
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| WebSocket 价格推送 | 标记价格实时更新 | ⬜ |
| PnL 实时变化 | 未实现盈亏随价格变动 | ⬜ |
| 风险等级变化 | 接近清算价时变红 | ⬜ |

---

## 五、历史记录 (History)

### 5.1 交易历史页面
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 访问 /history | 显示历史记录页面 | ⬜ |
| 调用 API | `GET /api/user/history` | ⬜ |
| 筛选功能 | 按类型筛选：全部/充值/提现/交易/盈亏 | ⬜ |
| 列表展示 | 时间、类型、金额、状态 | ⬜ |
| 分页/加载更多 | 支持 cursor 分页 | ⬜ |

### 5.2 历史记录详情
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 充值记录 | 显示 txHash，可跳转区块链浏览器 | ⬜ |
| 交易记录 | 显示开仓/平仓详情 | ⬜ |
| 盈亏记录 | 显示已实现盈亏金额 | ⬜ |

---

## 六、提现流程 (Withdraw)

### 6.1 提现入口
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 资产页点击「提现」 | 弹出提现弹窗 | ⬜ |
| 检查可提现余额 | 显示最大可提现金额（即可用余额） | ⬜ |

### 6.2 提现申请
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 输入提现金额 | 验证：0 < 金额 <= 可用余额 | ⬜ |
| 点击「提现」 | 调用 `POST /api/user/withdraw` | ⬜ |
| 后端处理 | 预扣可用余额，调用 Vault.withdraw() | ⬜ |
| MetaMask 弹出 | 显示合约调用交易 | ⬜ |
| 确认交易 | 交易发送，pending 状态 | ⬜ |

### 6.3 提现确认
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 链上确认 | Indexer 监听到 Withdraw 事件 | ⬜ |
| 数据库更新 | Transaction 记录更新为 CONFIRMED | ⬜ |
| 余额扣减 | 可用余额实际扣减 | ⬜ |
| 前端显示 | Toast 提示提现成功 | ⬜ |
| 历史记录 | /history 显示提现记录 | ⬜ |

---

## 七、错误处理与边界情况

### 7.1 网络错误
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 断开网络后登录 | 显示网络错误提示 | ⬜ |
| API 超时 | 显示超时提示，可重试 | ⬜ |
| WebSocket 断开 | 自动重连机制 | ⬜ |

### 7.2 余额不足场景
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 保证金 > 可用余额 | 下单按钮禁用，提示余额不足 | ⬜ |
| 提现 > 可用余额 | 表单验证失败 | ⬜ |

### 7.3 交易 rejected
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| MetaMask 拒绝签名 | 返回登录失败，不卡死 | ⬜ |
| MetaMask 拒绝交易 | 显示交易被拒绝 | ⬜ |

### 7.4 并发操作
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 快速连续点击下单 | 防重复提交，只发一次请求 | ⬜ |
| 同时开多个仓位 | 单标的单向单仓模式，第二个会合并？或拒绝？ | ⬜ |

---

## 八、Socket.IO 实时功能

### 8.1 连接管理
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 登录后自动连接 | Socket 连接成功 | ⬜ |
| Token 过期 | Socket 断开，提示重新登录 | ⬜ |
| 页面隐藏 | 保持连接或优雅降级 | ⬜ |

### 8.2 事件订阅
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 订阅市场行情 | 接收 `market:BTC:update` 事件 | ⬜ |
| 订阅个人仓位 | 接收 `position:{userId}:update` 事件 | ⬜ |
| 取消订阅 | 页面切换时正确取消 | ⬜ |

---

## 九、性能与体验

### 9.1 加载性能
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 首屏加载时间 | < 3s | ⬜ |
| 路由切换 | 客户端导航，无刷新 | ⬜ |
| 数据加载 | 骨架屏或 loading 状态 | ⬜ |

### 9.2 交互体验
| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 按钮 loading 状态 | 异步操作时有 loading 指示 | ⬜ |
| Toast 提示 | 操作成功/失败有反馈 | ⬜ |
| 表单验证 | 实时验证，错误提示清晰 | ⬜ |

---

## 十、测试通过标准

### 10.1 核心流程闭环
- [ ] 可以完成一次完整的 `充值 -> 开仓 -> 持仓 -> 平仓 -> 提现` 流程
- [ ] 每个环节都有明确的状态反馈
- [ ] 余额在各阶段计算正确

### 10.2 数据一致性
- [ ] 前端显示余额与后端 API 一致
- [ ] 仓位数据在各组件间同步
- [ ] 历史记录完整准确

### 10.3 错误处理
- [ ] 所有 API 错误有用户友好的提示
- [ ] 网络中断后可恢复
- [ ] 交易拒绝后状态正确

---

## 附录：API 端点速查

| 端点 | 方法 | 用途 |
|------|------|------|
| /api/auth/challenge | GET | 获取 SIWE nonce |
| /api/auth/verify | POST | 验证签名，获取 token |
| /api/auth/session | GET | 获取当前会话 |
| /api/auth/logout | POST | 登出 |
| /api/user/balance | GET | 查询余额 |
| /api/user/positions | GET | 查询仓位列表 |
| /api/user/history | GET | 查询历史记录 |
| /api/user/withdraw | POST | 发起提现 |
| /api/trade/order | POST | 创建订单（开仓） |
| /api/trade/positions/:id | DELETE | 平仓 |
| /api/trade/positions/:id | GET | 查询仓位详情 |
| /api/health | GET | 健康检查 |

## 附录：关键 Hook 检查点

| Hook | 关键功能 | 检查方式 |
|------|----------|----------|
| useAuth | 登录状态、SIWE 流程 | 观察登录流程是否正常 |
| useBalance | 余额查询、WebSocket 更新 | 资产页余额是否正确 |
| useOrders | 下单、平仓 | 交易功能是否正常 |
| usePositions | 仓位查询、实时更新 | PositionPanel 数据是否正确 |

---

**测试记录模板：**

```
日期: 2026-03-XX
测试人: XXX
环境: 本地 / Testnet
结果: 通过 / 部分通过 / 失败
问题列表:
1. xxx
2. xxx
```
